# Dependencies
node_modules
/.pnp
.pnp.js

# Testing
/coverage
playwright-report/
test-results/
.auth/

# Next.js
/.next/
/.next.stale-*/
/out/

# Production
/build

# Misc
.DS_Store
*.pem
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
.vercel
.env*.local
*.pyc
__pycache__/
backend/node_modules/
backend/data/
backend/logs/
completion
logs
run
sdk/node/dist/
sdk/python/dist/
sdk/python/dcp_provider.egg-info/

# Desktop app installers (served from production, not git)
backend/public/dcp-provider-setup.*

# TypeScript incremental build cache (root tsconfig)
dist/tsconfig.tsbuildinfo

# Claude agent sandboxes (local worktrees, never commit)
.claude/worktrees/
.claude/logs/

# Databases — never commit real or local SQLite files
*.db
*.sqlite
*.sqlite-journal
backend/database.sqlite
backend/dc1.db

# Internal docs — these live in dc1-platform-internal (private repo), not here
CLAUDE.md
AGENT_LOG.md
AGENTS.md
*AGENT-BRIEFING.md
*-HANDOVER.md
PAPERCLIP-*.md
FOUNDER-STRATEGIC-BRIEF.md

# Phase/sprint/status reports — use GitHub Issues, not committed markdown
PHASE*.md
SPRINT*.md
DEVOPS-*.md
DEPLOY-REQUEST-*.md
DEPLOY_REQUEST_*.md
CRITICAL-DCP-*.md
COPYWRITER-SPRINT*.md
2026-*-cost-report.md
2026-*-deployment-status.md
2026-*-sprint-report.md
2026-*-next-sprint-plan.md

# Half-finished R&D subsystems — commit on a dedicated feature branch,
# never let `git add -A` drag them into a launch build. (lib/*.ts are tracked frontend files — do NOT ignore them.)
lib/gbrain/
lib/perception/
lib/safety/
lib/verification/

# Local tooling state — never commit
.codegraph/
.herenow/
docs/**/*.bak-v1
